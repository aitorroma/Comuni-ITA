services:
  comuni-ita-api:
    container_name: comuni-ita-api
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "8080:8080"
    networks:
      - coolify
    labels:
      - coolify.managed=true
      - coolify.proxy=enabled
      - coolify.port=8080
      - traefik.http.services.comuni-hc8444csgc488ogwk48w0ggs.loadbalancer.server.port=8080
    volumes:
      - ./data:/app/data # Mount the data directory for easy updates
    restart: unless-stopped
    
networks:
  coolify:
    external: true
    name: coolify
